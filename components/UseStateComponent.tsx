import styled from '../styles/themed-components';

interface IProps {}

const StyledUseStateComponent = styled.div`
  
`;

const UseStateComponent = (props: IProps) => {
  const {} = props;
  return (
    <StyledUseStateComponent>
      UseStateComponent
    </StyledUseStateComponent>
  );
};

export default UseStateComponent;
